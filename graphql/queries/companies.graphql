extend type Query {
    """ Get a company by their ID """
    GetCompanyByID(id: ID! @eq): Company @find(model: "App\\Models\\Company")

    GetCompany: Company  @first(model: "App\\Models\\Company")
}
